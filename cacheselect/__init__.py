"""CacheSelect runtime package.

Benchmark workloads and their ground truth intentionally live in the sibling
``benchmarks`` package so runtime code cannot accidentally depend on evaluation
labels.
"""
