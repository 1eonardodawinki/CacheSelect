# The generalized approach: testing KV-cache staleness on a plain long LLM
# request (key-value retrieval), independent of any specific agent workload.
# See periodic_agents_approach/ for the earlier, agent-specific approach.
